name: CI

on: [pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build and test
      run: |
        gem install bundler
        bundle update
        rspec specs/functional/index.spec
